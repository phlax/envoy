--- a/build_defs/cpp_opts.bzl
+++ b/build_defs/cpp_opts.bzl
@@ -18,6 +18,7 @@
     ],
     "//conditions:default": [
         "-Wno-sign-compare",
+        "-Wno-deprecated-declarations",
     ],
 })
 
--- a/src/google/protobuf/io/BUILD.bazel
+++ b/src/google/protobuf/io/BUILD.bazel
@@ -162,6 +162,6 @@
         "@abseil-cpp//absl/log:absl_log",
     ] + select({
         "//build_defs:config_msvc": [],
-        "//conditions:default": ["@zlib"],
+        "//conditions:default": ["//:zlib"],
     }),
 )
--- a/BUILD.bazel
+++ b/BUILD.bazel
@@ -3,4 +3,5 @@
 load("@rules_cc//cc:defs.bzl", "cc_binary", "cc_library")
+load("@bazel_skylib//rules:common_settings.bzl", "label_flag")
 load("@rules_license//rules:license.bzl", "license")
 load("//bazel:cc_proto_library.bzl", "cc_proto_library")
 load("//bazel:java_lite_proto_library.bzl", "java_lite_proto_library")
@@ -806,5 +807,14 @@
 filegroup(
     name = "bzl_srcs",
     srcs = glob(["**/*.bzl"]),
     visibility = ["//visibility:public"],
 )
+
+# The zlib dependency used by protobuf's io library. Defaults to the same
+# zlib envoy otherwise selects so behavior is unchanged when this flag is
+# not overridden on the command line.
+label_flag(
+    name = "zlib",
+    build_setting_default = "@envoy//bazel:zlib",
+    visibility = ["//visibility:public"],
+)
